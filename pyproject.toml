[project]
name = "llama-cluster"
version = "0.1.0"
description = "AI Cluster UI with RPC backend"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "gradio>=4.0.0",
    "llama-cpp-python>=0.2.0",
]
